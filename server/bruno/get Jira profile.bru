meta {
  name: get Jira profile
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/auth/jira/profile
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{access_token}}
}

auth:bearer {
  token: {{access_token}}
}
